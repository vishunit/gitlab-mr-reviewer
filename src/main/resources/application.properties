# Example config - set values here OR pass via -D properties at runtime
# GitLab host (no trailing /api path)
gitlab.host=
# repository path (namespace/project), e.g. dfs-core/orchestration/soe
gitlab.repoPath=
# MR IID (project-scoped), e.g. 42
gitlab.mrIid=
# Your GitLab Personal Access Token (glpat...)
gitlab.token=

# Groq API config
groq.api.url=https://api.groq.com/openai/v1/chat/completions
groq.api.key=
groq.model=meta-llama/llama-4-scout-17b-16e-instruct

spring.profiles.active=local
